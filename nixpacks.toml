[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
# Use npm install instead of npm ci - no lockfile required
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

